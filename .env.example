# AI provider (server-side only — never expose to the client)
AI_PROVIDER=openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Future: swap provider without touching callers (see src/services/ai)
# ANTHROPIC_API_KEY=
# ANTHROPIC_MODEL=claude-opus-4-8
